# 로그인 페이지 UI/UX 개선 구현 계획서

## 1. 개요
사용자의 첫인상을 결정하는 로그인 페이지의 디자인을 더 현대적이고 깔끔하게 개선하고, 사용자 경험을 저해하는 요소(예: 브라우저 자동완성 배경색)를 수정합니다.

## 2. 주요 변경 사항

### 2.1 UI/UX 디자인 개선
- **카드형 레이아웃**: 로그인 폼 전체를 감싸는 흰색 카드 컨테이너를 도입하여 시각적 집중도를 높입니다.
    - `bg-white`, `rounded-2xl`, `shadow-sm`, `border` 적용.
- **배경색 변경**: 페이지 전체 배경을 기존 `bg-gray-50`에서 더 밝고 세련된 `bg-[#f9fafb]`로 변경합니다.
- **타이틀 및 폰트**: 'Pretendard' 스타일의 깔끔한 폰트 느낌을 위해 `tracking-tight` 및 `text-gray-900`을 적용합니다.

### 2.2 입력 필드 스타일링
- **개별 필드 분리**: 기존의 묶음형(merged) 입력 필드에서 개별 분리형으로 변경하여 가독성을 높입니다.
- **패딩 확대**: `py-3.5` 등의 클래스를 사용하여 입력 영역을 더 넓고 편안하게 만듭니다.
- **자동완성 스타일 오버라이드**: Chrome 등 브라우저의 기본 파란색 자동완성 배경을 제거하기 위해 `globals.css`에 `box-shadow: 0 0 0 1000px white inset` 규칙을 추가합니다.

### 2.3 시각적 구분선 (Divider)
- **디자인 개선**: "또는" 텍스트 주변의 선을 더 얇고(`border-gray-100`) 깔끔하게 처리하며, 텍스트 배경을 카드 색상(`bg-white`)과 일치시킵니다.

### 2.4 반응형 및 접근성
- **모바일 대응**: `max-w-[420px]`로 너비를 제한하되, 모바일에서는 패딩을 조정하여 유연하게 반응하도록 합니다.
- **접근성 준수**: Label과 Input 연결 (`htmlFor`, `id`) 및 적절한 포커스 링 스타일을 유지합니다.

## 3. 파일 변경 목록
- `src/app/login/page.tsx`: 전체적인 구조 및 클래스명 전면 개편.
- `src/app/globals.css`: 입력 필드 자동완성 관련 글로벌 스타일 추가.

## 4. 검증 계획
- [x] 로그인/회원가입 토글 시 UI 깨짐 없는지 확인
- [x] 카카오 로그인 버튼 스타일 유지 확인
- [x] 브라우저 자동완성 시 배경색이 흰색으로 유지되는지 확인
